# authnexus
AuthNexus is a production-style authentication system built using Spring Boot, JWT, Redis, and RBAC. It demonstrates secure session management using access and refresh tokens, token validation, and frontend integration with Axios interceptors.
